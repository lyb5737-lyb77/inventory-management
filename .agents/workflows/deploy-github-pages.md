---
description: deploy the application to GitHub Pages
---

1. Committing and pushing the changes to the `main` branch automatically triggers the `.github/workflows/deploy.yml` workflow.
2. Do NOT run `npm run deploy` or manually build for deployment.
